# torch installed separately via cu128 index (sm_120 support):
# uv pip install torch --index-url https://download.pytorch.org/whl/cu128
tiktoken>=0.7.0
numpy>=1.26
tqdm>=4.66
tensorboard>=2.17
pandas>=2.0        # finetune/classifier.py: SpamDataset CSV loading
transformers>=4.40 # model/pretrained.py: HF hub source for OpenAI GPT-2 weights
safetensors>=0.4
huggingface_hub>=0.24 # explicit pin — used directly for push_to_hub/login, not just as a transformers sub-dep
